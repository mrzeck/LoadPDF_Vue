export default {
    setFolder(state, data){ 
        state.folder = data;
    },
    setAccessToken(state,data){
        state.accessToken = data;
    },
    setFiles(state,data){
        state.files = data;
    }
}